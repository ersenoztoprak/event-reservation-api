version: "3.9"

services:
  app:
    build: .
    container_name: secure-ticketing-api
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    volumes:
      - h2-data:/data

volumes:
  h2-data:
